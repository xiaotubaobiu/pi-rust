# M3b harness 移植 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port the packages/agent harness subtree (~14.9k lines src: compaction 1297, execution 444, pico3 6038, env 924, root files 3548, session envelope included) to full upstream parity, completing packages/agent.

**Architecture:** New modules under `src/agent_core/harness/`. Two M6 dependencies resolved by faithful used-surface shims (established scope-cut precedent, disclosed per task): (1) `@earendil-works/chord` → `src/agent_core/chord_support.rs` (Context/ContextKey/BACKGROUND_CONTEXT DI + delta Ops/Path/Seg/Tracker/track/applyImmutable/isBase + JsonRepresentation — full chord port remains M6); (2) `@earendil-works/pi-telemetry` → typed-span definitions subset in the telemetry module. skills.ts needs `ignore` + `yaml` behavior (gitignore matching + YAML parse) — pinned Rust crates, disclosed.

**Tech Stack:** Existing deps + pinned new: `serde_yaml` (skills), gitignore-matching crate (TBD at task time). Upstream snapshot: C:/Users/13063/Desktop/code/pi @ 5901446.

**Spec:** upstream packages/agent/src/harness/* + test/harness/* (73 oracle test files, 24.8k lines) are the executable spec.

**Port style:** upstream authority; oracle tests ported per task; session envelope (pico3/session.ts 1497 lines) is the ROADMAP compat deliverable — byte-format compatibility with upstream session files is its acceptance gate.

## Global Constraints

- Edition 2021, no unsafe; gates per task: cargo test / clippy -D warnings / fmt; CI green at push.
- chord/pi-telemetry shims: faithful for the used surface; each shim type/function cites its upstream source; full package ports remain M6.
- Commit messages: `{feat,fix,docs,chore}: <message>`.

---

### Task 1: chord-support subset

**Files:** Create `src/agent_core/chord_support.rs` (+ submodules as needed).
**Upstream:** packages/chord/src used-surface (Context, ContextKey, BACKGROUND_CONTEXT from chord/context; Op, Path, Seg, Tracker, track, applyImmutable, isBase from chord/delta; JsonRepresentation, JsonValue) — read the chord src files for these exports IN FULL and port their semantics faithfully (immutable apply, path addressing, base comparison).
- [ ] Failing semantics tests (apply/path/base per upstream behavior) → implement → gates → commit `feat(agent-core): chord support subset (context + delta)`

### Task 2: harness foundation types

**Files:** Create `src/agent_core/harness/{types,events,messages,result,config,context}.rs`.
**Upstream:** harness/types.ts (407), events.ts (286), messages.ts (169), result.ts (117), config.ts (42), context.ts (37) — read all IN FULL.
- [ ] Failing tests → implement → gates → commit `feat(agent-core): harness foundation types`

### Task 3: skills + prompt-templates

**Files:** Create `src/agent_core/harness/{skills,prompt_templates}.rs`.
**Upstream:** skills.ts (396 — SKILL.md discovery/parse, ignore-file filtering, frontmatter), prompt-templates.ts (270).
- [ ] Failing tests (fixture skill dirs) → implement → gates → commit `feat(agent-core): skills and prompt templates`

### Task 4: hooks

**Files:** Create `src/agent_core/harness/hooks.rs`.
**Upstream:** hooks.ts (533 — read IN FULL).
- [ ] Failing tests → implement → gates → commit `feat(agent-core): harness hooks`

### Task 5: compaction

**Files:** Create `src/agent_core/harness/compaction/` (compaction.rs, branch_summarization.rs, utils.rs).
**Upstream:** compaction/ (1297 — read IN FULL) + oracle compaction.test.ts / branch-summarization.test.ts.
- [ ] Failing tests → implement → gates → commit `feat(agent-core): compaction`

### Task 6: execution + env

**Files:** Create `src/agent_core/harness/{execution/,env/}`.
**Upstream:** execution/ (444), env/ (924 — ExecutionEnv abstraction; nodejs.rs impl maps to tokio/fs).
- [ ] Failing tests → implement → gates → commit `feat(agent-core): execution primitives and env`

### Task 7: pico3 session envelope + jsonl

**Files:** Create `src/agent_core/harness/pico3/{session,jsonl}.rs`.
**Upstream:** pico3/session.ts (1497 — THE envelope format; ROADMAP compat deliverable), jsonl.ts (339).
**Acceptance:** session files written by the port load in upstream SessionManager semantics (envelope `{type,id,parentId,timestamp,message}`), and vice versa — byte-format round-trip tests against upstream-produced fixtures.
- [ ] Failing round-trip tests → implement → gates → commit `feat(agent-core): pico3 session envelope and jsonl`

### Task 8: pico3 types + memory + chord glue + view

**Files:** Create `src/agent_core/harness/pico3/{types,memory,chord,view}.rs`.
**Upstream:** pico3/types.ts (1038), memory.ts (278), chord.ts (178 — service defs), view.ts (477).
- [ ] Failing tests → implement → gates → commit `feat(agent-core): pico3 types, memory and view`

### Task 9: pico3 scheduler + system + harness

**Files:** Create `src/agent_core/harness/pico3/{scheduler,system,harness}.rs`.
**Upstream:** scheduler.ts (486), system.ts (376), harness.ts (812).
- [ ] Failing tests → implement → gates → commit `feat(agent-core): pico3 scheduler, system and harness`

### Task 10: agent-harness + telemetry

**Files:** Create `src/agent_core/harness/{agent_harness,telemetry}.rs`.
**Upstream:** agent-harness.ts (622), telemetry.ts (635 — typed spans over pi-telemetry; port the span-type definitions subset with the shim precedent).
- [ ] Failing tests → implement → gates → commit `feat(agent-core): agent harness and telemetry`

### Task 11: integration + oracle migration close

**Files:** Modify harness modules + any CLI touches.
**Oracle:** remaining test/harness/* oracles not yet ported (73 files total across tasks).
- [ ] Port remaining oracle coverage; full gates; push; CI green; commit `feat(agent-core): harness complete (M3b)`

---

## Self-Review Notes

- Full chord + pi-telemetry ports remain M6; shims cite upstream and are behavior-faithful for the used surface (precedent: ai-layer scope cuts, all disclosed).
- pico3/session envelope is the phase's compat centerpiece — its acceptance is upstream SessionManager semantics round-trip.
- Test trajectory: 1442 → ~1700+ by T11 (73 oracle files provide abundant coverage).
- Harness docs (packages/agent/docs/pico-v5*.md) are supplementary reading for T7-T9 implementers.
