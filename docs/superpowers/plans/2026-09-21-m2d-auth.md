# M2d 认证体系移植 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port the auth system to full upstream parity — core types/credential-store/resolve (616 lines), env-api-keys (188), all 9 OAuth provider flows (2973 lines), the auth.json file store + login/logout CLI surface (119), and the bedrock/vertex ambient chains deferred from M2c.

**Architecture:** `src/ai/auth/` module tree mirroring upstream packages/ai/src/auth/. CredentialStore as a Rust trait (read/list/modify/delete with serialized modify, per upstream types.ts:65-96); OAuth flows are async functions taking an `AuthInteraction` impl (prompts/notify/browser events — upstream types.ts:125-168) so interactivity is injectable and token-exchange HTTP is wiremock-testable. auth.json compatibility: `{providerId: {type: "api_key"|"oauth", ...}}` JSON map (ROADMAP §compat).

**Tech Stack:** Existing deps + reqwest for token endpoints; oauth flows' crypto: PKCE needs SHA-256 + base64url — use a small pinned crate if not already in tree (check; disclose). Upstream snapshot: C:/Users/13063/Desktop/code/pi @ 5901446.

**Spec:** `docs/superpowers/specs/2026-09-19-m2-pi-ai-full-port.md` (§1 M2d row)

**Port style:** upstream + 13 oracle test files (oauth-auth, oauth-device-code, anthropic-oauth, github-copilot-oauth, kimi-coding-oauth, openai-codex-oauth, openrouter-oauth, radius-oauth, xai-oauth, env-api-keys, bedrock-credentials, google-vertex-api-key-resolution, oauth.ts harness) are the executable spec. Browser-dependent oracle halves (real browser launch) are tested upstream via stubs — port the stubbed assertions.

## Global Constraints

- Edition 2021, no unsafe; gates per task: cargo test / clippy -D warnings / fmt; CI green at push.
- Wire-compat: auth.json round-trip must equal upstream's format; token-exchange request bodies/headers byte-pinned vs oracle expectations.
- Interactive surfaces (browser open, terminal prompts) go through `AuthInteraction` — flows never touch stdio/browser directly (mirrors upstream separation).
- Commit messages: `{feat,fix,docs,chore}: <message>`.

---

### Task 1: auth core types + InMemoryCredentialStore + env-api-keys

**Files:** Create `src/ai/auth/mod.rs`, `types.rs`, `credential_store.rs`, `env_api_keys.rs`; Test: in files.
**Oracle:** env-api-keys.test.ts. Port: Credential enum (ApiKeyCredential{key, env?}/OAuthCredential{...refresh fields per types.ts:24-36}), CredentialStore trait (read/list/modify/delete; modify = serialized read-modify-write callback), InMemoryCredentialStore, getApiKeyEnvVars provider→env-var table (types.ts:117, env-api-keys.ts full), AuthType/AuthInfo/AuthEvent/AuthPrompt types.
- [ ] Failing tests → implement → gates → commit `feat(ai): auth core types, credential store, env api keys`

### Task 2: resolveProviderAuth + AuthContext/helpers

**Files:** Create `src/ai/auth/resolve.rs`, `context.rs`, `helpers.rs`.
**Oracle:** oauth-auth.test.ts (resolve half). Port: ModelsError + ModelsErrorCode, AuthResolutionOverrides, resolveProviderAuth precedence (stored credential owns provider: env only when nothing stored; failed refresh never falls back to env — types.ts/README contract), AuthContext, helpers (envApiKeyAuth etc.).
- [ ] Failing tests → implement → gates → commit `feat(ai): resolveProviderAuth, auth context and helpers`

### Task 3: PKCE + oauth-page + anthropic oauth

**Files:** Create `src/ai/auth/oauth/mod.rs`, `pkce.rs`, `oauth_page.rs`, `anthropic.rs`.
**Oracle:** anthropic-oauth.test.ts. Flow: authorize URL build (code_challenge S256), token exchange, refresh, error shapes. Browser/prompt via AuthInteraction.
- [ ] Failing wire tests → implement → gates → commit `feat(ai): anthropic oauth flow`

### Task 4: openai-codex oauth

**Files:** Create `src/ai/auth/oauth/openai_codex.rs`.
**Oracle:** openai-codex-oauth.test.ts. JWT account-id extraction (already partly in codex API port — reuse), id_token claims, token exchange/refresh.
- [ ] Failing wire tests → implement → gates → commit `feat(ai): openai codex oauth flow`

### Task 5: github-copilot oauth

**Files:** Create `src/ai/auth/oauth/github_copilot.rs`.
**Oracle:** github-copilot-oauth.test.ts. Device flow (github/.login/device), polling interval semantics, exchange.
- [ ] Failing wire tests → implement → gates → commit `feat(ai): github copilot oauth flow`

### Task 6: openrouter + device-code + xai

**Files:** Create `src/ai/auth/oauth/{openrouter.rs, device_code.rs, xai.rs}`.
**Oracle:** openrouter-oauth.test.ts, oauth-device-code.test.ts, xai-oauth.test.ts. Generic device-code engine (device-code.ts) consumed by openrouter/xai.
- [ ] Failing wire tests → implement → gates → commit `feat(ai): openrouter, device-code and xai oauth flows`

### Task 7: kimi-coding + radius + load

**Files:** Create `src/ai/auth/oauth/{kimi_coding.rs, radius.rs, load.rs}`.
**Oracle:** kimi-coding-oauth.test.ts, radius-oauth.test.ts. load.ts = oauth registry/loader.
- [ ] Failing wire tests → implement → gates → commit `feat(ai): kimi-coding, radius oauth flows and oauth loader`

### Task 8: auth.json file store + cli login/logout + ambient chains

**Files:** Create `src/ai/auth/file_store.rs`, `src/ai/cli_auth.rs` (login/logout commands); Modify bedrock/vertex ambient chains to completion.
**Oracle:** bedrock-credentials.test.ts, google-vertex-api-key-resolution.test.ts (ambient halves), oauth.ts harness expectations. auth.json format per cli.ts loadAuth/saveAuth + types.ts:36 comment. Bedrock: AWS_ACCESS_KEY_ID/SECRET/SESSION_TOKEN env chain + profile precedence rules (upstream bedrock-converse-stream.ts:156-175, 1185+). Vertex: GOOGLE_APPLICATION_CREDENTIALS file key (upstream google-vertex.ts:424+).
- [ ] Failing tests → implement → gates → commit `feat(ai): auth.json file store, login/logout, ambient chains`

### Task 9: integration

**Files:** Modify `src/main.rs` (+`--auth` auth.json path, key resolution order: explicit → auth.json → env per upstream precedence), `src/config.rs`.
- [ ] Wire; gates; push; CI green; commit `feat(ai): auth wired into cli (M2d complete)`

---

## Self-Review Notes

- Models-collection integration of auth (models.stream resolving through owning provider) is M2e — M2d delivers building blocks + cli wiring.
- Interaction trait design lands in T1 (types) and is consumed by all flows; browser-open events are data (AuthEvent), never side effects.
- Test trajectory: 817 → ~950+ by T9.
